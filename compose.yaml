services:
  db:
    container_name: ScreenSound-DB
    hostname: ScreenSound-DB
    image: mcr.microsoft.com/mssql/server
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=[Senha123]
